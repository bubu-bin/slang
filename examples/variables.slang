/**
 * Variable declaration and assignment.
 */

// Assigning a string to a variable
homie myString = "value";
shout(myString);

// Assigning a number to a variable
homie myNumber = 2;
shout(myNumber);

// Copy the value of one variable to another
homie myVar = "myVar";
homie myVar2 = myVar;
shout(myVar2);
